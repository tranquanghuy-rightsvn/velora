/* Where the cart page sends a checkout submission (name/phone/email/address +
 * items) when a buyer confirms they've paid. Replace with the deployed GAS
 * web app's /exec URL — Apps Script editor: Deploy > Manage deployments.
 * See gas/README.md for the full setup. Left as a placeholder, checkout
 * fails with a friendly error telling the buyer to DM instead. */
window.VELORA_CHECKOUT_ENDPOINT = 'REPLACE_WITH_YOUR_GAS_WEB_APP_URL';
